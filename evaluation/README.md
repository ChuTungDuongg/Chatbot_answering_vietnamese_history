# Deterministic baseline evaluation

See [docs/EVALUATION.md](../docs/EVALUATION.md). This package reads saved SSE benchmark JSONL records and a versioned question set. It never imports the application or calls a model.
